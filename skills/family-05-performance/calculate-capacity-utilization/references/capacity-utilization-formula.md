# Capacity-utilization formula

Use `utilization = actual output or occupied time / rated available capacity or time` only when numerator and denominator share resource, period, product, and unit basis. A missing rated capacity is a missing denominator, not zero.
