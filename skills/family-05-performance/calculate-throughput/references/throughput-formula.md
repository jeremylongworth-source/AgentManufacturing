# Throughput formula

Use `throughput = completed quantity / elapsed time` for matching periods and units. Keep the completion definition and exclusions visible; completed output is not automatically good output or rated capacity.
