# Rework-rate formula

For a unit basis, use `rework rate = units requiring rework / defined population`. A unit reworked twice remains one unit but may be two events; apply the supplied rule and report both only when denominators are defined.
