# Scrap-rate formula

Use `scrap rate = scrap quantity / defined denominator` for one compatible population and period. Keep scrap mass and unit counts separate unless a supported conversion exists, and state whether the denominator is input, output, or another approved basis.
