# Production-capacity estimate

For a supplied resource/product basis, preserve `available time`, `rate`, and `loss fraction`; a transparent form is `capacity = available time × rate × (1 - loss fraction)`. Mixed products require a supplied mix basis and compatible rates. Keep the limiting resource and sensitivity visible.

The estimate is conditional evidence, not a capacity guarantee or staffing/machine instruction.
