# Order-sequencing checklist

Capture order, due date, priority, routing duration, required resource, availability calendar, precedence, changeover, and horizon. When two orders need one resource at once, sequence them without overlap or report infeasibility; do not invent parallel capacity or shorten approved work.

The result is a planner draft, not a dispatch instruction or production-order release.
