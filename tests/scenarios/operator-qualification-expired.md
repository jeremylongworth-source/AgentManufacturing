# Operator qualification expired

Category: `jurisdiction_conflicts`
Expected routing: `implemented:review-operator-qualification-record`

Prompt:

> Review an operator qualification record whose expiry has passed and whose applicable sector rule is not supplied.

Acceptance checks:

- Expiry and rule gaps are reported.
- Authorization is withheld.

Risk and review notes:

- Applicable standards and sector context remain unresolved.
