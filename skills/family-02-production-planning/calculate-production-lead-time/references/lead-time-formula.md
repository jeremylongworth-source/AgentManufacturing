# Production lead-time formula

For non-overlapping components, use `lead time = queue + setup + run + transfer`. If setup overlaps queue, subtract only the overlap that is explicitly supported, and show the convention and every intermediate. Preserve measured and assumed components separately.

The estimate supports planning analysis. It is not a customer promise, schedule release, or capacity guarantee.
