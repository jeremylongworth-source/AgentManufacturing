# First-pass-yield formula

Use `first-pass yield = units accepted without rework / initial units entering operation`. Reworked units that later pass are excluded from the numerator. Preserve population, period, exclusions, units, and denominator validation.
